BUG FIX + UX FIX BUNDLE (do all items below, nothing else):
1) Follow-up rounds must update the Report Viewer + PDF as Report V1/V2/V3
2) PDF must show the Appendix 3 Output Disclaimer at the bottom of EVERY page
3) My Studies: show most recent 10 + pagination + search by Title
4) My Personas: show most recent 10 + pagination + search by Persona ID (and keep citations usability)

Constraints:
- Do NOT reset/delete DB
- Do NOT change QA logic, grounding traces, telemetry, exports, budgets, persona system rules
- Keep token-based sessions working
- Make minimal, targeted edits

========================================
(1) FOLLOW-UP REPORT VERSIONING (V1/V2/V3)
========================================
Problem:
Follow-up questions are stored and executed but the Report Viewer doesn’t reflect them.
Need V1 (initial), V2 (after follow-up #1), V3 (after follow-up #2).

Required behavior:
- Completed study with no follow-ups => Report V1
- After follow-up round 1 => Report V2 (includes follow-up 1 appended)
- After follow-up round 2 => Report V3 (includes follow-up 1 and 2 appended)
- Report Viewer defaults to the latest version.
- Provide a version selector (dropdown or tabs): V1 / V2 / V3 (show only available)
- PDF download must support version selection via query param ?version= (default latest)

Implementation:
A) build_structured_report(study, version):
- Build base 5-section report from the original study output/brief.
- If version >=2: append a new section at end:
  "Follow-up Round 1" containing user question + follow-up output (and QA status/notes if present)
- If version >=3: append "Follow-up Round 2" likewise.
B) UI:
- In output view, add "Report Version" selector shown only when follow-ups exist.
- Changing version rerenders report on screen.
C) PDF:
- /download-pdf/<id>?version=2 generates V2 PDF; default is latest.
- Ensure “raw output” section stays collapsible; report is the primary view.

========================================
(2) PDF DISCLAIMER FOOTER ON EVERY PAGE
========================================
Problem:
PDF currently missing the standard Output Disclaimer. Must appear on every page.

Requirement:
Add footer text at bottom of EVERY PDF page with EXACT disclaimer from Appendix 3:

Disclaimer
This output was generated by an experimental AI‑powered simulation system. All personas, behaviours, and insights are synthetic and do not represent real individuals or guaranteed real‑world outcomes. Results are provided for exploratory purposes only and should not be relied upon as professional, legal, financial, or investment advice.

Implementation (fpdf2):
- Create a PDF class that subclasses FPDF and overrides footer() to render disclaimer.
- Ensure footer wraps (multi_cell) and never overlaps body:
  - increase bottom margin and enable auto_page_break with that margin
- Footer must appear on EVERY page including multi-page reports.
- Disclaimer is English: use Helvetica/core font is fine even if report uses CJK fonts.

Bonus (optional but recommended):
- Add the same disclaimer block at the bottom of the on-screen Report Viewer too (not just PDF).

========================================
(3) MY STUDIES: RECENT 10 + PAGINATION + SEARCH BY TITLE
========================================
Problem:
My Studies list is too long.

Requirements:
- Show only most recent 10 studies by default.
- Add pagination (Prev/Next + Page X of Y).
- Add a search box that filters studies by Title (case-insensitive contains match).
- Search results must also be paginated at 10 per page.
- Maintain existing buttons (“Open Study”, etc.).

Implementation:
- Use query params: ?studies_page=1&studies_q=...
- SQL:
  - WHERE title LIKE %q% (case-insensitive)
  - ORDER BY created_at DESC (or id DESC if created_at missing)
  - LIMIT 10 OFFSET (page-1)*10
- Count total rows for pagination.

========================================
(4) MY PERSONAS: RECENT 10 + PAGINATION + SEARCH BY PERSONA ID
========================================
Problem:
My Personas list is too long and hard to locate personas referenced by ID.

Requirements:
- Show only most recent 10 personas by default.
- Add pagination (Prev/Next + Page X of Y).
- Add search box that filters personas by persona identifier / id (case-insensitive contains match).
  - Primary: ID search
  - Also allow searching by persona name if you already have it (optional)
- Search results paginated at 10 per page.

Implementation:
- Use query params: ?personas_page=1&personas_q=...
- SQL:
  - WHERE persona_id LIKE %q% OR id LIKE %q% (depending on schema)
  - optionally OR name LIKE %q%
  - ORDER BY created_at DESC (or id DESC)
  - LIMIT 10 OFFSET (page-1)*10
- Count total rows for pagination.

Deliverables:
- Files changed
- How to test

TEST A: Report versions
1) Run IDI/FG to completion => Report selector shows only V1.
2) Submit follow-up #1 => selector shows V1/V2, defaults to V2. V2 includes follow-up section.
3) Submit follow-up #2 => selector shows V1/V2/V3, defaults to V3. V3 includes both follow-ups.
4) Download PDFs with ?version=1 and ?version=3 => contents differ correctly.

TEST B: PDF disclaimer on every page
1) Create a long report that spans multiple pages (or use a dev test generator).
2) Download PDF and verify disclaimer footer appears on every page.

TEST C: My Studies search + pagination
1) Ensure >12 studies exist.
2) Dashboard shows 10 most recent.
3) Next/Prev works.
4) Search by title substring => results filtered and paginated.

TEST D: My Personas search + pagination
1) Ensure >12 personas exist.
2) Personas view shows 10 most recent.
3) Next/Prev works.
4) Search by Persona ID substring (e.g., “12”) => filtered and paginated.