REPLIT AGENT — FIX Prompt 3 regressions (manual save buttons still visible + no extraction/saving)

FILES YOU MAY EDIT
- artifacts/brainstorm/app.py
- templates/index.html

DO NOT
- Change SQLite schema
- Add new tables/columns
- Add new workflows beyond Prompt 3
- Change Ben precheck rules

GOAL
Make Prompt 3 reliable:
- No “Save as …” buttons anywhere
- Mark proposals reliably produce a green “Confirm & Save” button
- Clicking Confirm saves the extracted field deterministically

1) Fix _parse_proposed_update() robustness (CRITICAL)
Current parser is too brittle (regex fails on minor format drift).
Replace with a line-based parser:
- Only parse Mark’s last message
- Require “Should I save these updates?” to be present
- Find “- field:” line, capture the key
- Find “value:” line (allow leading spaces), capture text until:
  - “Confirmation question:” OR
  - “Should I save these updates?”
Return {field,value} if valid.
Must accept slight whitespace differences.

2) Remove system prompt contradictions
In Mark system prompt:
- Remove or rewrite the lines that say “End after your single question” / “no footer”
- Replace with a single rule:
  “You MUST end with Proposed updates block + the confirmation question.
   The confirmation question is your only question.”
Do NOT reintroduce “Suggested saves”.

3) Add server-side fallback if Mark doesn’t output Proposed updates
In the worker writeback path (or immediately after Mark output is received):
- If Mark output does NOT contain “Proposed updates:”
- And there is a clear next-missing V1A anchor
- Then append a minimal Proposed updates block proposing that anchor
  with value = the last user message (trimmed).
Apply a lightweight confidence gate:
- Only do this if last Mark question contains the anchor label OR
  the anchor is next-missing and user message length <= 200 chars.

4) Eliminate any remaining manual save UI
In templates/index.html:
- Ensure no remaining blocks render “Save as …” buttons (chat_save_buttons or get_save_buttons)
- Ensure no forms call /save-chat-field from the chat UI or brief UI

TESTING
A) Manual buttons gone:
- Open draft study
- Ensure no “Save as …” buttons appear anywhere

B) Confirm button appears:
- Send: “We’re focused on Japan, mainly urban consumers.”
- Mark reply must include Proposed updates OR fallback must add it
- Confirm & Save button appears

C) Saving works:
- Click Confirm & Save
- Page refresh shows Market/Geography saved
- Mark posts “Saved: Market / Geography.”

DELIVERABLE
Files changed:
- …
What changed:
- …
How to test:
1) …
2) …
3) …

