SITE-WIDE THEME + TYPOGRAPHY + PASSWORD RULE + BRAND-FIRST UPDATE (Do ONLY this)
Implement ONLY the changes below. Do not implement new prompts/features.

Goals:
1) Typography:
   A) Default English font: Calibri (with fallbacks)
   B) Japanese font stack: NO web import (system fonts)
   C) Chinese Simplified stack: PingFang SC with cross-platform fallbacks
   D) Chinese Traditional stack: PingFang TC with cross-platform fallbacks
   E) Unified mixed-page stack: English pages should keep Calibri for Latin, but render JP/CN glyphs cleanly via fallbacks.

2) Password rule:
   - Enforce password length min 6, max 10 characters (server-side + UI)

3) New color scheme (site-wide):
   - Alternate backgrounds: White #FFFFFF and Grey Blue #5C7E8F (replace any #064273 usage)
   - Button/complements: Grey #A2A2A2 and Light Grey #D4DDE2
   - Ensure other colors complement this palette (avoid clashing saturated colors)

4) Branding rule (brand-first):
   - On branded pages (landing, blog, dashboard, admin), the brand/product name must be a hero-level signal.
   - The brand wordmark/logo lockup must be more visually prominent than any headline.
   - No headline should overpower the brand.
   - If logo asset is not available, use a text-only wordmark placeholder, but structure HTML/CSS so a logo image can be dropped in later without redesign.

Hard constraints:
- Do NOT change study/chat/QA/report/model config/upload logic.
- Do NOT reset/delete DB.
- Keep routing behavior (unauth / -> landing, auth / -> dashboard).
- Apply consistently across landing, blog pages, dashboard, admin pages, and shared templates.

================================================
A) Typography + language-specific font switching
================================================
1) Implement language persistence:
- Store selected language code in session or cookie when user changes language dropdown.
- Apply it as data attribute on <html> (preferred): <html data-lang="en"> etc.

Language options (V1) must remain exactly:
- English (value "en")
- CN-Simplified简 (value "zh-Hans")
- CN-Traditional繁 (value "zh-Hant")
- Japanese日 (value "ja")

2) Define font stacks (no web imports required for JP/CN; use system fallbacks)
Add CSS variables or direct rules:

ENGLISH LATIN stack:
  Calibri, "Segoe UI", Arial, sans-serif

JAPANESE stack (NO web import):
  "Hiragino Sans",
  "Hiragino Kaku Gothic ProN",
  "Yu Gothic",
  Meiryo,
  "MS PGothic",
  sans-serif

CN Simplified stack (cross-platform):
  "PingFang SC",
  "Hiragino Sans GB",
  "Microsoft YaHei UI",
  "Microsoft YaHei",
  sans-serif

CN Traditional stack (cross-platform):
  "PingFang TC",
  "Microsoft JhengHei UI",
  "Microsoft JhengHei",
  sans-serif

3) Apply per-language font-family:
- html[data-lang="ja"] -> Japanese stack
- html[data-lang="zh-Hans"] -> CN Simplified stack
- html[data-lang="zh-Hant"] -> CN Traditional stack

4) Unified mixed-page font stack for English pages:
For html[data-lang="en"], use a combined stack so Latin stays Calibri but JP/CN glyphs render cleanly:
font-family:
  Calibri, "Segoe UI", Arial,
  "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Yu Gothic", Meiryo, "MS PGothic",
  "PingFang SC", "PingFang TC", "Hiragino Sans GB",
  "Microsoft YaHei UI", "Microsoft YaHei",
  "Microsoft JhengHei UI", "Microsoft JhengHei",
  sans-serif;

================================================
B) Password length rule (min 6, max 10)
================================================
Enforce wherever passwords are set/changed:

1) Sign Up form:
- Client-side: password input minlength=6 maxlength=10
- Server-side: reject if len(password) < 6 or > 10 with message:
  “Password must be 6–10 characters.”

2) Change password form (if exists):
- Same rule server-side + UI attributes.

Do NOT change hashing/auth logic.

================================================
C) Color scheme update (site-wide)
================================================
1) Define CSS variables globally:
:root {
  --pb-white: #FFFFFF;
  --pb-accent: #5C7E8F;     /* grey blue */
  --pb-grey: #A2A2A2;       /* primary buttons / accents */
  --pb-lightgrey: #D4DDE2;  /* secondary surfaces */
  --pb-text: #111111;
  --pb-text-on-accent: #FFFFFF;
}

2) Replace any #064273 usage with var(--pb-accent).
- Landing alternating backgrounds must be white and #5C7E8F.

3) Buttons:
- Primary button bg: --pb-grey, text white
- Secondary button bg: --pb-lightgrey, text #111
- Hover states stay within palette (slightly darker/lighter greys).

4) Ensure all legacy colors (including old ocean blue) are replaced where relevant.

================================================
D) Brand-first rule implementation (no logo file required yet)
================================================
1) Create a reusable "brand lockup" component in templates used on:
- landing page
- blog list and blog post view
- dashboard header
- admin header

2) Brand lockup must be hero-level:
- Visually larger than any headline on the page.
- On landing page hero: the largest text element should be the brand wordmark:
   "Project Brainstorm"
  and the existing hero headline (“Simulate Markets. Avoid Costly Mistakes.”) must be smaller (e.g., h2).
- In headers on other pages: show brand name prominently (not tiny nav text).

3) Logo slot:
- Add a placeholder <img> for logo at /static/brand/logo.svg (or .png) BUT:
  - If file not present, hide the img cleanly and show text-only wordmark.
  - Do not break layout if logo missing.

4) Styling:
- Brand wordmark uses bold weight, high contrast, and is larger than headlines.
- Ensure brand remains readable on both white and accent backgrounds.

================================================
E) Deliverables + tests
================================================
Deliverables:
- Files changed

How to test:

TEST 1: Fonts
1) Default (English): Calibri for Latin; verify mixed text renders Japanese/Chinese glyphs correctly via fallback.
2) Switch to Japanese日 -> uses Hiragino/Meiryo stack.
3) Switch to CN-Simplified简 -> PingFang SC stack (fallback to YaHei on Windows).
4) Switch to CN-Traditional繁 -> PingFang TC stack (fallback to JhengHei on Windows).
5) Refresh -> selection persists and fonts remain correct.

TEST 2: Password rule
- 5-char password rejected
- 11-char password rejected
- 6–10 accepted

TEST 3: Palette
- Landing alternates #FFFFFF and #5C7E8F (no #064273)
- Buttons use #A2A2A2 / #D4DDE2 consistently

TEST 4: Brand-first
- Landing: “Project Brainstorm” visually larger than “Simulate Markets…” headline.
- Blog and dashboard: brand lockup clearly visible and not just tiny nav text.
- Logo placeholder does not break UI if file not present.