PROMPT 3L: 
CODING AGENT PROMPT — FIX MISSING GREEN CONFIRM BUTTON (FINAL)
Objective (Non Negotiable)
Fix the recurring failure where Mark emits text that looks like a proposal, but the UI never shows the green “Confirm & Save” button.
The fix must be deterministic, enforced server side, and fully test covered.
Prompt rules alone are not sufficient.

Root Cause (Authoritative)
The system currently allows Mark to emit illegal mixed turns:
•	Mark asks a question
•	In the same message, Mark also emits a Proposed updates: block
Even if the text looks like a proposal, the system must treat this as a question turn, not a proposal turn. As a result:
•	The proposal is ignored
•	No parsed proposal exists
•	The UI never enters confirmation state
•	The green button never appears
•	The user types “yes save” into a void
This is correct behavior — but the system must recover automatically instead of dead ending.

Required Invariants (Must Be Enforced in Code)
1.	Question and proposal are mutually exclusive
o	If a Mark message contains a ?, it is a question turn
o	A proposal is illegal in that same message
2.	A proposal only exists if it is parseable
o	Exact format required
o	Proposal only turn
o	Successfully parsed by policy_parse_last_mark_proposal
3.	If Mark emits an illegal or malformed proposal, the system must force a clean retry
o	No silent failure
o	No auto save
o	Single retry only

Implementation Tasks (Do All of These)
1️⃣ Add a Hard Server Side Guard (MANDATORY)
Create a wrapper around proposal parsing.
def parse_mark_proposal_or_none(mark_text: str):
# HARD RULE: a proposal cannot coexist with a question
if "?" in mark_text and "Proposed updates:" in mark_text:
return None
return policy_parse_last_mark_proposal(mark_text)

✅ Replace all direct calls to policy_parse_last_mark_proposal with this wrapper.
This guarantees:
•	Mixed turns are always rejected
•	The system never enters an ambiguous state

2️⃣ Add Automatic Recovery for Illegal / Malformed Proposals
Immediately after Mark produces a reply, add this logic:
parsed = parse_mark_proposal_or_none(mark_reply)

if "Proposed updates:" in mark_reply and parsed is None:
# Mark attempted an illegal or malformed proposal — force retry
chat.append({
"role": "mark",
"content": (
"I attempted to propose an update incorrectly. "
"Let me restate it properly."
)
})

return re_invoke_mark(
chat,
system_override=(
"SYSTEM: Emit the proposal again using the exact mandatory format. "
"Do NOT ask a question. Emit ONLY the proposal block and the "
"confirmation question."
)
)

Constraints:
•	✅ Single retry only
•	✅ No loops
•	✅ No silent saves
•	✅ Proposal still goes through confidence gating
•	✅ User must still click the green button

3️⃣ Lock This with ONE Golden Path Integration Test (MANDATORY)
Add this exact test (or equivalent) to your integration test suite.

def test_golden_path_mixed_turn_recovers_to_clean_proposal():
chat = []

# Step 1: Mark asks a question
chat.append({
"role": "mark",
"content": "What is the business problem in one clear sentence?"
})

# Step 2: User answers
chat.append({
"role": "user",
"content": "I want to sell more running shoes but I don't know how and where"
})

# Step 3: Mark emits illegal mixed turn (question + proposal)
bad_mark_reply = (
"Thanks. What is the business problem?\n\n"
"Proposed updates:\n"
"- field: business_problem\n"
" value: I want to sell more running shoes but I don't know how and where\n"
"Confirmation question:\n"
"Should I save these updates?"
)
chat.append({"role": "mark", "content": bad_mark_reply})

# Step 4: Worker handles the reply
chat = handle_mark_reply(chat)

# Step 5: Final Mark message must be a clean, parseable proposal
final_mark_reply = chat[-1]["content"]
parsed = policy_parse_last_mark_proposal(final_mark_reply)

assert parsed is not None
assert parsed.field == "business_problem"

This test must fail before the fix and pass after.

Expected Correct Runtime Behavior (Acceptance Criteria)
After this fix:
1.	Mark asks a question
2.	User answers
3.	If Mark emits an illegal mixed turn:
o	System rejects it
o	System forces Mark to retry cleanly
4.	Mark emits a proposal only message in exact format
5.	✅ Green “Confirm & Save” button appears
6.	User must click the button to save
7.	No text based “yes save” confirmations are supported (by design)
 
Explicit Non Goals (Do NOT Do These)
•	❌ Do NOT auto save user content
•	❌ Do NOT accept text confirmations like “yes save”
•	❌ Do NOT weaken proposal parsing rules
•	❌ Do NOT change UI logic
•	❌ Do NOT rely on prompt rules alone
 
Definition of Done
•	Illegal mixed turns cannot dead end the system
•	Every valid proposal produces a green button
•	The golden path integration test passes
•	No regressions in existing proposal flow
 
Final Note to the Agent
This is not an LLM quality problem.
This is a protocol enforcement problem.
Enforce the protocol. Force recovery. Ship it.
