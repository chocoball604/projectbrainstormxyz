# Overview

This is a pnpm workspace monorepo using TypeScript, designed for building AI-Native Market Research applications. The project aims to provide a robust and scalable platform for conducting various types of market research, including synthetic surveys, in-depth interviews (IDIs), and focus groups, leveraging AI for efficiency and advanced insights.

The core of the project is the "Brainstorm App," a Python Flask application that facilitates the entire research lifecycle from study creation and persona management to report generation and follow-up analysis. This platform focuses on streamlining the market research process, enabling rapid iteration, and delivering actionable intelligence.

Key capabilities include:
- Token-based authentication for secure access.
- Comprehensive study management with various statuses and types.
- AI-powered persona generation and management.
- Detailed grounding trace logging for transparency.
- Cost telemetry and budget enforcement for resource management.
- Admin functionalities for model configuration, web sources, and usage monitoring.
- Document grounding library for attaching and managing research materials.
- Structured report generation with PDF export.
- Follow-up mechanisms for iterative research.
- Usage metering and limits to manage resource consumption.
- Health checks for integrated AI models.

The project emphasizes a modular architecture, utilizing pnpm workspaces to manage different applications and shared libraries, ensuring maintainability and scalability.

# User Preferences

I want iterative development.
Ask before making major changes.
I prefer detailed explanations.
Do not make changes to the folder `brainstorm_v1_replit_singlepage_pack/`.
Do not make changes to the file `artifacts/brainstorm/brainstorm.db`.

# System Architecture

The project is structured as a pnpm workspace monorepo. It leverages Node.js 24 and TypeScript 5.9 for backend services and shared libraries.

**Core Applications:**
- **`api-server`**: An Express 5 API server handling primary API requests, utilizing `@workspace/api-zod` for validation and `@workspace/db` for data persistence.
- **`brainstorm`**: A Python Flask + SQLite single-page application focused on AI-Native Market Research. It manages studies, personas, reports, and user interactions.

**Shared Libraries (`lib/`):**
- **`api-spec`**: Manages the OpenAPI 3.1 specification (`openapi.yaml`) and Orval configuration for API client and schema generation.
- **`api-client-react`**: Generates React Query hooks and a fetch client from the OpenAPI spec for frontend consumption.
- **`api-zod`**: Generates Zod schemas from the OpenAPI spec for request and response validation, ensuring data integrity across services.
- **`db`**: Encapsulates the database layer using Drizzle ORM with PostgreSQL, providing a centralized way to interact with the database schema.

**Build and Type-checking:**
- **`esbuild`**: Used for bundling CJS modules.
- **TypeScript Composite Projects**: All packages extend a base `tsconfig.base.json` with `composite: true`, enabling efficient type-checking and dependency resolution across the monorepo. Type-checking is performed from the root using `tsc --build --emitDeclarationOnly`.

**Feature Specifications (Brainstorm App):**
- **Authentication**: Token-based authentication (URL query parameter `?token=xxx`).
- **Database**: Uses SQLite with a predefined set of tables for users, sessions, studies, personas, chat messages, cost telemetry, and more.
- **Study Types**: Supports `synthetic_survey`, `synthetic_idi`, and `synthetic_focus_group`, each with specific configurations and limits.
- **Persona Management**: Immutable personas with unique `persona_instance_id`s; cloning creates new instances.
- **Grounding Traces**: Records detailed traces of AI model interactions, including sources used. MLG Step 1 populates traces with real retrieval data (tier-level reason codes) for persona creation.
- **MLG (Minimal Live Grounding)**: Live web retrieval during persona creation and study execution (IDI/FG only). Uses 3-tier priority: user uploads → admin uploads → model-mediated web search. **Patch 2.1.2**: All live web retrieval for product outputs is mediated via OpenAI Responses API with `web_search_preview` (model-mediated web search). DuckDuckGo (`ddgs`), `requests.get` URL fetching, and all application-side ranking/scoring/filtering have been removed from product paths. Admin-configured web sources are injected as retrieval hints in the model prompt, not fetched directly. Tier stats and reason codes use vendor-neutral naming (`model_web_search`). Wikipedia helpers (`_mlg_is_wikipedia`, `_mlg_is_secondary_authoritative`) are retained as informational only — never used for ranking or exclusion. Ben QA fails if deprecated origins ("Local Non-English", "General Web", "Admin-Directed") or deprecated tier names ("local_web:", "general_web:", "admin_web:") appear in grounding data. Admin OpenRouter model import (`/admin/import-openrouter-models`) is the sole allowed exception for direct URL fetch (admin-only, not product-facing). Step 2 adds execution-level calibration (injected into study prompts as "Population Calibration Context (NOT evidence)"). Execution MLG data persisted in `studies.exec_grounding_data` column for report surfacing. **Patch 2.1 — Context Citations**: OpenAI Responses API with `web_search_preview` retrieves 3-5 journalism articles as context sources (NOT evidence). Uses Product/Concept + Business Problem only (never demographics). Stored in `exec_grounding_data.context_sources`. Injected into Lisa prompts as "[Context — NOT evidence]" block. Report section 8.5 "Context Sources (NOT Evidence)" rendered in web view, PDF, and translation pipeline. Ben QA enforces: citation_class must be "context", no overlap with grounding URLs, prohibited phrases check for treating context as evidence. Env vars: `AI_INTEGRATIONS_OPENAI_BASE_URL`, `AI_INTEGRATIONS_OPENAI_API_KEY`.
- **Admin Features**: Includes capabilities for managing admin web sources, configuring AI models (`mark_model`, `lisa_model`, `ben_model`), managing allowed models, and editing persona model pools.
- **Reporting**: Generates structured reports with confidence labels and supports PDF download.
- **Security Hardening V1A — P1 batch (Task #56)**: Medium-priority gaps from the Task #55 audit closed. (1) **ProxyFix wired** — `app.wsgi_app = ProxyFix(app.wsgi_app, x_for=1, x_proto=1, x_host=1)` so X-Forwarded-Proto/For from Replit's proxy is honored; `_client_ip()` now uses the proxy-populated `request.remote_addr` again, restoring per-client rate-limiter lockouts instead of per-instance ones. (2) **Secure session cookie** — new `pb_session` cookie (HttpOnly, SameSite=Lax, Secure in production) is set on `/login`, `/signup`, `/verify-signup`, and `/admin-login`; `get_token()` prefers the cookie and falls back to the URL/form `token` param for one release so existing sessions do not break; `/logout` clears it. Flask's own `SESSION_COOKIE_SECURE/HTTPONLY/SAMESITE` flags also configured; `MAX_CONTENT_LENGTH=16MB` set as a safety cap. (3) **CSRF tokens on every state-changing POST** — no Flask-WTF; a per-browser token stored in a `pb_csrf` cookie (readable by JS so AJAX can forward it), exposed to Jinja as `csrf_token` and via a `{{ csrf_input() }}` global that renders a hidden input. A `before_request` hook rejects POST/PUT/PATCH/DELETE without a matching `csrf_token` form field or `X-CSRF-Token` header (compared via `secrets.compare_digest`). Only the internal `/__health` endpoint is allowlisted — login/signup/admin-login POSTs also enforce CSRF because their landing/login/portal GET page reliably sets the `pb_csrf` cookie first. Templates were mechanically updated: every `<form method="POST">` in `index.html` (60), `landing.html` (2), `account.html` (2), `admin_portal.html` (2), and `verify_email.html` (1) now includes `{{ csrf_input() }}`. `index.html` and `admin_step1_library.html` ship a small fetch() wrapper that auto-attaches `X-CSRF-Token` on same-origin POST/PUT/PATCH/DELETE (reading the `pb_csrf` cookie), covering all 33 existing fetch() calls without touching them individually. Dynamically-constructed form HTML in JS strings was also rewritten to embed the CSRF input. (4) **Security response headers via `@app.after_request`** — every response now carries `Content-Security-Policy` (with per-request nonce exposed as `g.csp_nonce`/`csp_nonce`; kept pragmatic with `'unsafe-inline'` + `script-src-attr 'unsafe-inline'` because ~57 inline event handlers remain in index.html — nonce paves the way for a stricter policy later), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=()`, and (production only) `Strict-Transport-Security: max-age=31536000; includeSubDomains`. `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, and `frame-ancestors 'none'` provide real defense-in-depth even with the loose script directives. **Deviation from the task plan**: the CSP is *nonce-aware* but not nonce-*strict* — adding nonces to every inline `<script>` block while also rewriting the ~57 inline `onclick=`/`onsubmit=` handlers was out of scope for this batch; the groundwork is laid for a follow-up tightening. **No product behavior changes**: existing URL-token sessions continue to authenticate. Remaining Task #55 items (upload MIME sniffing + EXIF strip, LLM output caps + safe JSON, run-study idempotency, central auth decorators, server-side length caps, audit log, automated security tests) deferred to the P2 batch.
- **Security Hardening V1A — P0 batch (Task #55, partial)**: Three highest-risk security gaps closed for the beta release. (1) **Fail-fast secret validation** — at import time, when `FLASK_ENV != "development"` (the default), the app now `sys.exit(1)` with a clear FATAL message if `FLASK_SECRET` is missing or set to a known-bad default (`""`, `"dev-fallback-key"`, `"changeme"`, `"secret"`, `"default"`), or if `ADMIN_PASSWORD` is missing or set to a known-bad default (`""`, `"admin"`, `"admin123"`, `"password"`, `"changeme"`, `"default"`). Local dev keeps using the existing dev secrets via `FLASK_ENV=development` set in `artifact.toml`'s `[services.env]`. `app.run(..., debug=...)` now passes `debug=(not IS_PRODUCTION)` so the Werkzeug debugger (RCE risk) is gated behind dev mode only. (2) **Brute-force throttling on `/login`, `/admin-login`, `/signup`** — added a small in-memory token-bucket limiter (`check_auth_rate_limit`, `record_auth_failure`, `clear_auth_failures`) keyed by client IP + identity. Per-IP windows: login/admin = 10/min, signup = 10/hr. Per-(IP, identity) failure lockouts: login = 5 fails/15min → 15min lockout; admin = 5 fails/15min → 30min lockout; signup = 10/hr → 30min lockout. Lockouts return HTTP 429 with `Retry-After`; successful auth clears the failure counter. Limiter is in-memory only (does not span instances; documented as a beta limitation). (3) **Blog stored-XSS fix** — `templates/blog_post.html:177` switched from `{{ post.body | safe }}` to `{{ post.body | sanitize_html | safe }}`. New Jinja filter `sanitize_html` calls `bleach.clean` with an allowlist of tags (`p,h1-h6,ul,ol,li,a,strong,em,b,i,u,code,pre,blockquote,hr,span,div,br,img,figure,figcaption`), attributes (`a[href,title,target,rel]`, `img[src,alt,title,width,height]`, `class` on a few container tags), and protocols (`http,https,mailto`); `<script>` tags are stripped, `javascript:` URIs are dropped, and `onerror=` attributes are removed. Added `bleach` to `pyproject.toml` deps. **No product behavior changes**: existing user sessions, all save/run flows, and Mark/Lisa/Ben pipelines are untouched. Remaining Task #55 items (CSRF, security headers, ProxyFix, secure session cookies, upload sniffing, run-study idempotency, central auth decorators, server-side length caps, audit log, blog body re-render, tests) deferred to follow-up batches.
- **Survey Setup (Prompt 8 — UI-only V1A)**: Four consolidated UI/workflow changes for `study_type == 'synthetic_survey'` configure pages. (1) Respondent count cap raised from 100 to 500 — `/save-survey-config`, the precheck clamp, `ben_precheck()` survey validation, and the `STUDY_TYPE_LIMITS["synthetic_survey"]["max_respondents"]` constant all now allow 25–500 (UI labels and Study Type Limits copy updated to match); defaults stay 100/8 and `question_count` (1–12) is unchanged. (2) Layout reorder — Survey Setup (counts + Survey Questions) now renders **above** Grounding Documents, Personas (still shows the existing "Not applicable for surveys" line), and Optional Context in the main column, so question-writing leads instead of supporting panels. Right-rail Brief So Far / Required to run / Precheck card is unchanged. Non-survey study types keep the original order. (3) Survey Purpose framing strip — a small reminder box renders immediately above the Survey Questions list inside Survey Setup with two display-only sentences: "Each question should help reduce uncertainty for this decision:" followed by the saved Decision to Support, then "If a question doesn't change your confidence in the decision, consider removing it." Reads from the existing Core Framing template variable; no new fields, no behavior change. (4) Question wizard — each Q1..Qn slot is collapsed by default ("Q{i} — Not configured / Configure Q{i}"); already-saved questions render as a collapsed saved-summary line with an "Edit Q{i}" button and the existing Saved badge / green background preserved. Expanding a card reveals an inline 3-step wizard: **Step 1 Signal** (Measure prevalence or intensity / Compare options or trade-offs / Understand reasons or motivations), **Step 2 Format** (signal-filtered: prevalence→Likert/Range/MC, compare→A/B/A·B Image/MC, reasons→Open/MC), **Step 3 Question text** which reuses the existing prompt textarea, hidden `.sq-type-select`, and per-type controls so save payloads to `/save-survey-question/<id>/<idx>` and `/save-survey-questions` are byte-identical. Inline non-blocking guardrails appear under the prompt: double-barrel ("and"/"or" splitting into ≥2 clauses), leading-word bias (helpful/improved/best/responsible), short prompt (<12 chars), and an open-ended sparingly tip. Save is **never blocked**. One-card-expanded-at-a-time enforced via `sqCollapseAll()` before expand. **No backend changes** — `/save-survey-config`, `/save-survey-question`, `/save-survey-questions`, `_normalize_survey_question`, `_validate_survey_questions`, `VALID_SURVEY_Q_TYPES`, persona/Lisa/Ben/Mark/telemetry pipelines all untouched.
- **Follow-ups**: Backend supports up to two follow-up rounds for completed IDI/FG studies. **Prompt 7 (V1A safety lock)**: Post-execution follow-up input UI is gated — on completed FG/IDI results pages there is no input box, no "N remaining" counter, no Submit button, and no "max reached" message. A short explanatory message is shown instead ("Follow-up questions are temporarily unavailable in V1A…"). Historical follow-up rounds (if any) still render above the message. Backend route `/submit-followup/<study_id>`, the `followups` table, `MAX_FOLLOWUP_ROUNDS`, `generate_followup_placeholder`, and all QA/report code paths are intentionally untouched so the gate is fully reversible (UI-only change).
- **Usage Monitoring**: Implements monthly usage meters and enforces free-tier limits for study creation.
- **Document Management**: Provides a document grounding library for user and admin uploads, with file size and storage limits.
- **Model Health Checks**: Includes a system for daily model health checks and weekly QA reports for AI models. Admin LLM smoke test endpoint at `/admin/llm-smoke` (POST) for quick single-model verification.
- **Marketing Landing Page**: A dedicated landing page (`landing.html`) for unauthenticated users, providing an overview of the platform.
- **Blog/News**: A blogging platform with public list and detail views, admin capabilities for post creation and image uploads, post pinning (up to 3 pinned posts with rank 1-3), and paginated blog listing (10 per page). Pinned posts appear first in rank order on page 1, followed by unpinned posts in reverse chronological order. Language selector on landing page shows 4 options: English, CN-Simplified简, CN-Traditional繁, Japanese日.
- **Manage Account**: Logged-in users can access `/account` to edit profile (name, company, role, location, LinkedIn) and change password (6–10 chars, current password required). Link in dashboard header.
- **Email Verification**: Enforced via `before_request` hook on all authenticated routes (except login/signup/logout/landing/blog/verify-email). Users without a verified email (or whose verification expired after 7 days) are redirected to `/verify-email`. Prototype shows 6-digit code inline (production would email it). Verification timestamp stored in `users.last_email_verification_timestamp`.
- **LLM Integration**: `call_llm()` uses OpenAI SDK pointed at Replit AI Integrations (OpenRouter). Env vars: `AI_INTEGRATIONS_OPENROUTER_BASE_URL`, `AI_INTEGRATIONS_OPENROUTER_API_KEY`. Falls back to `NotImplementedError` if env vars missing.
- **Branding & Theme**: Brand lockup (Enso logo `static/brand/enso_logo_256.png` + "Project Brainstorm" wordmark) in all page headers. CJK wordmark variant ("Project Brainstorm 集思廣益") shown for zh-Hans/zh-Hant/ja languages via client-side JS. CSS variables define palette: `--pb-accent: #5C7E8F`, `--pb-grey: #A2A2A2`, `--pb-lightgrey: #D4DDE2`. Typography uses Calibri base with CJK-specific font stacks via `html[data-lang]` selectors. Language persisted in `pb_lang` cookie, read by Flask context processor `inject_lang()` and applied as `data-lang` attribute on `<html>`. Password validation enforces 6–10 characters (backend + frontend `maxlength=10`).
- **3-State Mark Presence Model (Prompt 6 — UI/routing only)**: A new `ui_phase` variable (`STEP_1_FRAMING` / `STEP_2_ANCHORS` / `STEP_3_EXECUTION_READY`) is derived once per configure-study render from existing study state and governs Mark tile presence only — never alters Mark replies. **State 1** (no `study_type`): the original "Mark — Problem Framing Helper" tile renders unchanged (Quick Actions + chat input + transcript + all Step 1 framing prompts and telemetry). **State 2** (`study_type` set, study still in `draft` status): the tile collapses to a minimised "Mark — Brief Consistency Helper (optional)" with three alignment buttons only ("Check alignment with Business Problem", "Check alignment with Decision", "Remind me what question this study is answering") — no `<textarea>`, no Send button, no chat transcript, no Quick Actions. The Core Framing card from Prompt 5 stays visible. A passing precheck (`qa_status == 'precheck_passed'`) intentionally **stays in State 2** so the Brief Consistency Helper remains available right up until the user actually starts the study. **State 3** (`status` not in `('draft',)` — i.e. study has been started): Mark tile is not rendered at all. State 2 buttons POST to a new tightly-scoped `/mark-alignment-check/<study_id>` route which loads the saved BP/DS, calls `call_llm()` with a separate additive system prompt that hard-bounds the reply to a single short evaluative sentence (no rewriting, no method/execution suggestions, no follow-up questions), and returns JSON `{ok, reply}`. The new route closes the DB connection before `call_llm()` (V1A locking rule) and **never emits Step 1 telemetry events** — alignment-check is a separate, non-Step-1 intent. Existing Mark Step 1 prompts, `/send-chat`, Ben QA, persona generation, Lisa execution, and the four Step 1 telemetry hooks are mechanically untouched. Regression test: `test_prompt6_mark_presence.py` (5 tests) covers all three phases, the alignment-check contract, and a telemetry event-type drift guard identical to Prompt 5's.
- **Framing Continuity (Prompt 5 — UI/workflow only)**: Once a study type is set, a collapsible **Core Framing** card at the top of the Research Brief keeps Business Problem and Decision to Support visible and editable (read-only by default with an **Edit framing** button that re-enables the textareas using the existing `/save-discovery` autosave + checkpoint plumbing — no schema or request-shape changes). Market / Geography, Product / Concept, Target Audience, and the Optional Context fields stay as `<textarea>` forms after their first save (the JS handlers now flip a "Saved ✓ HH:MM" badge instead of swapping the form for a read-only block). The **Start Study** click now goes through a `window.confirm()` warning that anchors will be locked. Mark prompts, Mark reasoning, Ben QA scoring, persona generation, execution, and the four Step 1 telemetry hooks are mechanically untouched.
- **Step 1 Telemetry (Prompts 3 & 4 — mechanics only)**: Append-only `step1_telemetry_events` SQLite table records four observational signals (`pattern_triggered`, `template_applied`, `quick_action_used`, `rewrite_count_at_save`) emitted by `/save-discovery`, `send_chat`, and `mark_reply_worker`. The writer (`step1_telemetry.record_step1_event`) opens a short-lived sqlite connection, executes a single INSERT, never raises, and never holds the DB open across LLM/HTTP calls. **Kill switch**: `STEP1_TELEMETRY_ENABLED` env var (default `"1"`; values `"0"`/`"false"`/`"no"`/`"off"` disable). When off, no rows are written and `summarize_recent` returns a zero-state dict — Step 1 framing and Mark behavior are bit-for-bit identical with telemetry on or off. **Soft latency log**: `STEP1_TEL_SLOW_WRITE` is printed (observational only, no raise) when a single write exceeds `STEP1_TELEMETRY_SLOW_MS` (default 250 ms). **Session scoping** uses the per-study, per-page-load fallback explicitly permitted by Prompt 4 §2: `step1_session_id` is generated as `uuid4().hex` at study creation in `app.py` and threaded through every form POST. A future upgrade to a true "first-edit-of-BP-or-DS to Save-checkpoint OR navigate-away" lifecycle is captured as a follow-up. Admin dashboard at `/admin/step1-telemetry` renders read-only summaries; entry point lives in the Admin panel under "Step 1 Coaching Telemetry". Telemetry data must never be used to alter UX or Mark behavior — that belongs to Stage 3 Adaptive UX and requires a separate prompt.

# External Dependencies

- **Node.js**: Version 24
- **pnpm**: Package manager
- **TypeScript**: Version 5.9
- **Express**: Version 5 (API framework)
- **PostgreSQL**: Database
- **Drizzle ORM**: Object-relational mapper for PostgreSQL
- **Zod**: Validation library (`zod/v4`)
- **`drizzle-zod`**: Integration for Zod with Drizzle ORM
- **Orval**: API codegen tool (from OpenAPI spec)
- **esbuild**: JavaScript bundler
- **Python Flask**: Web framework for the Brainstorm app
- **SQLite**: Database for the Brainstorm app
- **fpdf2**: Python library for PDF generation (used in Brainstorm app)
- **React Query**: For client-side data fetching and caching (in `api-client-react`)
- **OpenRouter**: Integrated for AI model selection and management (`replit_openrouter`)